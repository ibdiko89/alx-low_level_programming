#include "main.h"

/**
 * reset_to_98 - updates the value of the variable
 * the pointer points to to 98 and resets it
 * @n: pointer to the variable to update
 * it run through the  4 stages of compiling. uses betty -style
 */
void reset_to_98(int *n)
{
	*n = 98;
}
